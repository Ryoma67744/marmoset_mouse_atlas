# Marmoset Brain Atlas Viewer 利用者ガイド

本書は **Marmoset Brain Atlas Viewer** を研究利用者として使うための手引きです。インストール作業は不要で、ブラウザから URL を開くだけで使い始められます。

> デプロイ設定・コード構成など開発者向けの情報は GitHub リポジトリを直接ご参照ください。

---

## 目次

1. [このアプリでできること](#1-このアプリでできること)
2. [はじめに：動作環境と起動](#2-はじめに動作環境と起動)
3. [Master 画面：データ登録と一括管理](#3-master-画面データ登録と一括管理)
4. [画面の見かた](#4-画面の見かた)
5. [基本ワークフロー](#5-基本ワークフロー)
6. [画像の重ね合わせと表示調整](#6-画像の重ね合わせと表示調整)
7. [HE と MSI の位置合わせ（重ね合わせ）](#7-he-と-msi-の位置合わせ重ね合わせ)
8. [ROI の表示・選択・手描き追加](#8-roi-の表示選択手描き追加)
9. [ANALYSIS（棒グラフ）で比較する](#9-analysis棒グラフで比較する)
10. [ZIP の書き出しと読み込み](#10-zip-の書き出しと読み込み)
11. [クラウド保存（複数の PC で同じデータを開く）](#11-クラウド保存複数の-pc-で同じデータを開く)
12. [キーボードショートカット](#12-キーボードショートカット)

---

## 1. このアプリでできること

- 手元の **imzML + ibd** と **HE 染色画像** をブラウザから **登録** し、そのまま観察できます。
- コモンマーモセット／マウス脳の **空間メタボロミクス（MSI）** と **組織染色画像（HE・IF）** を同じ空間で重ね合わせて観察できます。
- HE と MSI の **位置合わせ（自動／半自動）** を画面上で実行できます。
- あらかじめ登録された **ROI（関心領域）** に加え、画面上で **手描きの ROI** を追加登録できます。
- 選んだ ROI 内での **化合物の平均強度** をその場で棒グラフ比較できます。
- **第2階層のフォルダごと**に、同じ測定・散布バッチの切片へ **5-HT / d4-5-HT 画素比**と
  **D4 参照による DA・NE の切片間正規化**を適用し、生値と切り替えて確認できます。
- 検証済み検量線を登録したときだけ、**5-HT の ROI 単位の絶対定量値**を算出できます。
- 登録したデータを **フォルダ（入れ子可）** で整理できます。
- 作業状態ごと **ZIP に書き出し**、あとから **読み込んで続きから作業** できます。
- 全フォルダの MSI 生強度と ROI 情報を、データごとの **Excel ファイル**
  にして 1 つの ZIP で一括ダウンロードできます。

---

## 2. はじめに：動作環境と起動

### 2-1. 推奨ブラウザ

- Google Chrome / Microsoft Edge / Mozilla Firefox / Safari のいずれも最新版
- 画面幅は **1024px 以上** を推奨（モバイル表示は未最適化）

### 2-2. 公開サイトを開く

ブラウザで次の URL を開きます。

```
https://ryoma67744.github.io/marmoset_mouse_atlas/
```

ルート URL は **Master 画面**（データ登録と一括管理）です。ここでデータを登録し、
登録済みのものを開きます。**あらかじめ用意されたデータはありません** — 最初にご自身の
imzML/ibd と HE 画像を登録するか、以前 Export した ZIP を読み込んでください。

> 登録したデータは **お使いのブラウザ内（IndexedDB）** に保存されます。別の PC やブラウザに
> 持っていくときは ZIP に書き出してください（[10 章](#10-zip-の書き出しと読み込み)）。

### 2-3. バージョン表示

Master 画面・ビューアとも、**画面左上のタイトルのすぐ右**にアプリのバージョン（例 **`v2.10.0`**）が表示されます。不具合のご連絡やお問い合わせの際は、この数値を添えてください。

最新の修正が反映されていないように見えるときも、まずこの数値をご確認ください。番号が古いままであれば `Ctrl+F5`（Windows）または `Cmd+Shift+R`（macOS）で強制再読み込みしてください。

番号は `MAJOR.MINOR.PATCH` の 3 つ組で、真ん中は機能の追加・仕様の変更、右端は不具合の修正だけのときに上がります。

| バージョン | 主な内容 |
|---|---|
| 2.10.0 | 第2階層フォルダとその全子孫を1つの補正グループとして自動選択。グループごとに固定基準・表示レンジを保存し、追加・移動・設定混在を確認。Viewer・Excel・復元用 ZIP・クラウドにグループの記録を追加 |
| 2.9.0 | 5-HT の画素比、DA・NE の固定 D4 参照による切片間正規化、検証済み検量線による 5-HT ROI 絶対定量を追加。Viewer で生値／補正・正規化を切り替え、Excel に導出値・ROI 定量・根拠メタデータを別シートで出力。Otsu は確認必須の表示専用マスクとし、ROI/Excel の数値からは除外しない |
| 2.8.0 | 全フォルダの登録データを、1 データ = 1 Excel ファイルとして 1 つの ZIP にまとめる「全データをExcel一括出力」を追加。Excel には MSI 生強度、ROI の 0/1 情報、メタデータを収録。索引 Excel にはデータごとの分子名一覧も収録 |
| 2.7.0 | MSI 画像にスケールバーを表示するようにした（長さの根拠は imzML の画素サイズ。既に登録済みのデータでも操作なしで出る）。表示中の画像をスケールバー付き PNG で保存するボタンを追加 |
| 2.6.0 | 切片の外へ放射状に伸びる縞を出さないようにした。ImageQuest から書き出した imzML は、測っていない場所にも「隣の測定結果のコピー」を並べている。読み込み時にこれを見分けて除外する。表示だけでなく強度レンジ・背景除去・ROI の平均・位置合わせ・CSV/ZIP にも同じように効く。旧版で登録済みのデータは「中身」→「測定範囲を修復」で直せる |
| 2.5.0 | 手描き ROI の頂点を 1 つずつ取り消せるようにした（ROI List の「1 点戻す」／`Ctrl+Z`・`Cmd+Z`） |
| 2.4.0 | 登録済みデータの中身（分子・HE・Atlas・免疫染色）を一覧の「中身」から 1 つずつ追加・削除・差し替えできるようにした |
| 2.3.0 | 免疫染色画像の登録枠を追加（Atlas と同じ参照専用。サムネイルを押すと左半分が切り替わる）。同じ名前の ROI を 1 つとして扱い、まとめて平均を取るようにした |
| 2.2.0 | Atlas 画像の登録枠を追加。重ね合わせず、ビューアの左半分に並べて表示する |
| 2.1.0 | 表示画像を回転できるようにした（全体／HEのみ／MSIのみ）。角度はデータごとに保存される |
| 2.0.0 | 保存先をクラウド（Supabase）にした。どの PC で開いても同じ一覧が出る |
| 1.7.0 | 「全データ削除」ボタンを撤去し、削除はデータごと／選択したぶんだけにした |
| 1.6.0 | TIC を登録・表示しないようにした。透明度を同じ種類のレイヤ全体に効かせた。MSI の着色をカラーマップから分子ごとの固定色に戻した（線形・補間なしはそのまま） |
| 1.5.0 | レイヤの checkbox を横並びにし、強度レンジ／透明度を常時表示。MSI の表示方法を変更（1 分子ならカラーマップ、複数なら分子色の加算合成／明るさは線形／拡大しても補間しない）。HE のモノクロ表示を追加 |
| 1.4.0 | バージョン表示を追加 |
| 1.3.0 | ファイル名に `_` が無いと 1 ファイル＝1 データセットになる問題を修正。グループ分けの切り替え／データセット名の編集／登録済みデータの統合を追加 |
| 1.2.0 | 登録データをフォルダで整理できるようにした |
| 1.1.0 | 同梱データセットを撤去し、登録したデータ専用にした |
| 1.0.0 | imzML/HE の登録・重ね合わせ・レイヤの checkbox 表示・ZIP の往復 |

---

## 3. Master 画面：データ登録と一括管理

ルート URL（`https://ryoma67744.github.io/marmoset_mouse_atlas/`）が Master 画面です。

### 3-1. MSI データを登録する（imzML + ibd）

1. **① MSI データ** の「ファイルを選択」から、分子ごとの `.imzML` と **同名の** `.ibd` を
   まとめて選びます（ドラッグ＆ドロップも可）。3〜4 分子を一度に選べます。
2. ファイル名は次の規則で読み取られます。

   ```
   <分子名>_<方向>_<切片ID>.imzML
   例) NE_Cor_1_10.imzML → 分子 NE / コロナル切片 / 切片 1_10
   ```

   - 先頭の `_` より前が **分子名**（`NE`, `DA`, `5-HT` …）
   - 2 つ目の `_` の前が `Cor` なら **コロナル切片**、`Sag` なら **サジタル切片**
   - 残り（`1_10`）が切片の識別子。**ここが同じファイルが 1 つのデータセットにまとまります**

   **この規則に従っていなくても構いません。** `NE.imzML` `DA.imzML` のように `_` を
   含まない名前のときは、**選んだファイルすべてが 1 つのデータセットにまとまります**
   （ファイル名からは切片を区別できないため）。

   > **TIC は登録しません。** 分子名が `TIC` のファイルは、選んでも読み飛ばします
   > （分子の像ではないため）。表示マスク（[6-6](#6-6-表示マスクotsu)）は、
   > Master で明示選択した登録済み内因性分子の **生値の和**を使います。
   > フルスペクトルから求めた TIC ではありません。

3. 認識結果が表示されるので確認します。`.imzML` と `.ibd` の
   **ファイル名（拡張子より前）が一致していないとペアになりません**。

   - **データセット名は入力欄でその場で変更できます**（例: `10-1`）
   - 意図せず複数に分かれてしまったときは、**「すべて 1 つのデータにまとめる」**を選ぶと
     選んだファイルが 1 件にまとまります。逆に「ファイル名で切片ごとに分ける」で元に戻せます
   - 2 つ以上に分かれているときは画面に警告が出ます
4. **② HE 染色画像**（任意）を選びます。`.tif` / `.tiff`（LZW 圧縮を含む）/ `.png` / `.jpg` に対応します。
5. **③ Atlas 画像** / **④ 免疫染色画像**（任意）を選びます（[3-2](#3-2-参照用の画像を登録するatlas--免疫染色任意)）。
6. **登録する** を押します。解析の進捗がバーで表示されます。

> 1 ピクセルの値は、そのスペクトルの **intensity 配列の総和** として計算されます。
> グリッドサイズとピクセルサイズは imzML の `scanSettings` から読み取ります。
> zlib 圧縮された imzML には対応していません（エラーになります）。

### 3-2. 参照用の画像を登録する（Atlas / 免疫染色・任意）

**③ Atlas 画像** と **④ 免疫染色画像** には、**見比べるための画像**を 1 枚ずつ登録できます。
PNG / JPEG / TIFF に対応します。

| 枠 | 想定 |
| --- | --- |
| **③ Atlas 画像** | 領域名の入った脳図など |
| **④ 免疫染色画像** | 免疫染色の画像 |

どちらも扱いは同じです。

- **MSI や HE には重ねません。** Viewer の **左半分**に表示され、右半分に
  これまでどおりの重ね合わせが出ます。
- レイヤのチェックボックスには出ません。強度レンジ・透明度・モノクロ・Otsu 表示マスク、
  ANALYSIS の比較対象、いずれにも関わりません。**純粋に見比べるためだけの画像**です。
- **IMAGES の左端**に `Atlas` → `免疫染色` の順で並びます。
- **左半分に映すものは、IMAGES のサムネイルを押すと切り替わります。**
  いま映しているほうにはオレンジ色の枠が付きます。
- どちらも登録していないデータでは左半分は現れず、**これまでどおり全幅**で表示されます。
- ZIP に書き出しても保たれ、クラウド経由で他の PC からも同じように見えます。

### 3-3. 登録済みデータの管理

画面は左右に分かれています。**左がフォルダのツリー**、**右が選んだフォルダの中身**です。
一覧では、分子・グリッドサイズ・HE の有無・整合済みかどうか・更新日時が確認できます。

| 操作 | 内容 |
| --- | --- |
| **開く** | Viewer で表示する |
| **中身** | 分子・画像を 1 つずつ追加／削除／差し替える（[3-4](#3-4-登録済みデータの中身を編集する)） |
| **名前変更** | データセット名を変更する |
| **移動** | 別のフォルダへ移す |
| チェック 2 件以上 + **選択を 1 つに統合** | 分子ごとに分かれてしまったデータを 1 件にまとめる |
| **Export** | その 1 データの作業状態を、再読み込みできる ZIP に書き出す |
| **削除** | このデータを削除する |
| チェックボックス + **選択を移動 / 削除** | 選んだデータだけをまとめて処理する |
| チェックボックス + **選択を Export** | 選んだデータの復元用 ZIP を、1 データずつ書き出す |
| **全データをExcel一括出力** | フォルダやチェックの有無にかかわらず、すべての登録データの解析用 Excel を 1 つの ZIP で書き出す |
| **ZIP を読み込む** | 書き出した ZIP から復元する |

> 削除は**データごと**、またはチェックを付けた**選択したぶんだけ**です。
> まとめて全部消すボタンはありません（取り消せない操作のため）。
>
> クラウド保存を使っている場合、削除は**すべての PC から消えます**。詳しくは
> [11. クラウド保存](#11-クラウド保存複数の-pc-で同じデータを開く) を参照してください。

### 3-4. 登録済みデータの中身を編集する

一覧の **`中身`** を押すと、そのデータが持っている**分子と画像を 1 つずつ**
追加・削除・差し替えできます。登録し直さなくても、あとから足したり外したりできます。

| したいこと | 操作 |
| --- | --- |
| 分子を 1 つ消す（例: 5-HT だけ） | その分子の行の **`削除`** |
| 分子をあとから足す | **`+ 分子を追加 (imzML + ibd)`** で `.imzML` と同名の `.ibd` を選ぶ |
| HE / Atlas / 免疫染色を足す | その枠の **`追加`** |
| 画像を撮り直したものに変える | その枠の **`差し替え`** |
| 画像を外す | その枠の **`削除`** |

変更はその場で保存され、クラウドを使っていれば**そのつど送り直されます**
（中身が変わると本体の ZIP が変わるため、1 操作ごとにアップロードが走ります）。

> ⚠️ **編集や補正設定を行う前に、対象データを開いている Viewer タブをすべて閉じてください。**
> 古いバージョンを開いたタブも含みます。開いたままだと、Viewer 側が古い内容を
> 書き戻し、削除した分子や過去のプロファイルに戻ることがあります。

**気をつけること**

- **最後の 1 分子は消せません。** 分子が 0 個になると、ZIP に書き出したときに
  CSV がヘッダだけになり、ROI の列まで消えてしまうためです。
  データごと不要なときは一覧の `削除` を使ってください。
- **HE を消す・差し替えると、HE と MSI の位置合わせも消えます**（やり直しになります）。
  古い HE に合わせた変換が別の画像に掛かるのを防ぐためです。
- 既にある分子と**同じ名前**の分子を足すと `NE (2)` のように連番が付きます。
  この名前には決まった色が無いため**白黒で表示**されます。
- **グリッドの違う imzML は追加できません**（同じ切片ではないため）。その分子だけ飛ばし、
  ほかは追加されます。
- MSI のサムネイルは **4 分子まで**しか並びません。5 つ目以降はレイヤの
  チェックボックスには出ますが、下の IMAGES には出ません。

**測定範囲**

パネルの一番下に **測定範囲** の欄があります。

ImageQuest から書き出した imzML には、**実際に測っていない場所のデータも入っています**。
測定範囲は切片の形に沿った不定形ですが、imzML は長方形でないと書けないため、
余った隅を**いちばん近い実測点のコピー**で埋めているためです。
コピーなので値は本物そっくりですが、測定の実体はありません。

これをそのまま描くと、切片の縁から外へ**放射状・扇状の縞**が伸びます
（コピー元が扇形に広がるため）。手元のデータでは 10,680 座標のうち
**2,212 座標（20.7%）** がこのコピーでした。

v2.6.0 以降は、**登録するときに自動で見分けて除外します**。表示だけでなく、
強度レンジの上限・Otsu 入力・ROI の平均・位置合わせ・CSV/ZIP のすべてから外れます。
欄には `登録時に走査番号から判定済み: 実測 8,468 点 / 全 10,680 座標` のように出ます。

**v2.5.0 以前に登録したデータ**は、保存済みの数値そのものにコピーが入ったままなので、
自動では直りません。欄に **`未判定`** と出ている場合は、**`測定範囲を修復`** を押してください。

> ⚠️ 修復は**取り消せません**。また、保存済みの数値には走査番号が残っていないため、
> 「値が完全に一致する画素はコピー」という手掛かりだけで判定する**推定**です。
> 元の imzML から登録し直した場合と比べると一致度は **0.99 程度**で、
> 実測を数十画素落とし、コピーが数十画素残ります（見た目の放射状の縞は消えます）。
> **厳密にやりたいときは、元の imzML/ibd から登録し直してください。**
> ROI と位置合わせはそのまま残ります。

### 3-5. フォルダで整理する

データが増えてきたら、フォルダを作って整理できます。**階層の深さに制限はありません**。

```
📚 すべて
└── 📁 マーモセット
    └── 📁 2024年度
        ├── 📁 コロナル切片   ← ここに登録データが入る
        └── 📁 サジタル切片
```

| やりたいこと | 操作 |
| --- | --- |
| フォルダを作る | 開いているフォルダの中に **`+ フォルダ`** で作られます |
| フォルダに入る | 左のツリーをクリック、または右の一覧のフォルダ名をクリック |
| 上の階層に戻る | 右上の**パンくず**（`すべて / マーモセット / 2024年度`）をクリック |
| 開閉する | ツリーの **▶ / ▼** をクリック |
| データを移す | 行を**ドラッグして**ツリーやフォルダにドロップ、または **`移動`** ボタンで行き先を選ぶ |
| まとめて移す | チェックを付けて **`選択を移動`**（ドラッグでもまとめて運べます） |
| フォルダを移す | フォルダもドラッグ、または `移動` ボタンで移せます |

ツリーの右の数字は、**そのフォルダ以下にある登録データの合計件数**です（サブフォルダの分も含みます）。

> **新しく登録したデータは、そのとき開いているフォルダに入ります。** 先にフォルダを開いてから
> 登録すると、あとから移動する手間が省けます。

> **フォルダを削除しても、中のデータは消えません。** 中身（サブフォルダと登録データ）は
> 1 つ上の階層に繰り上がります。データを消したいときは、データ側の `削除` を使ってください。

### 3-6. 分子ごとに分かれてしまったときは

1 つの切片のはずなのに **分子ごとに別々のデータとして登録されてしまった**場合は、
対象にチェックを入れて **`選択を 1 つに統合`** を押してください。分子（レイヤ）が
1 件にまとめられ、元の行は 1 つになります。**データは失われません。**

- グリッドサイズが違うものは統合できません（同じ切片ではないため）
- HE 染色を持つデータが統合先になります
- 位置合わせ済みのものがあれば、その結果も引き継がれます

### 3-7. 補正・正規化を設定する

Master 画面の **`補正・正規化の設定`** で、**第2階層のフォルダごと**にプロファイルを
まとめて作ります。この操作は**生値を変更しません**。

「すべて」は0階層です。`Marmoset / Coronal` なら `Marmoset` が第1階層、
`Coronal` が第2階層で、**Coronal 直下と、その配下の全サブフォルダのデータ**が1つの補正グループです。

| データの場所 | 補正グループ |
| --- | --- |
| `Marmoset / Coronal` の直下・配下 | `Marmoset / Coronal` |
| `Marmoset / Sagittal` の直下・配下 | `Marmoset / Sagittal` |
| `Mouse / Coronal` の直下・配下 | `Mouse / Coronal`。Marmoset 側とは別グループ |
| 「すべて」直下・第1階層直下 | 第2階層に属さないため、新しい補正グループの対象外 |

件数は実際の階層から集計します。Master の一覧が直下だけを表示している場合も、
**補正対象には子孫を含む全件が入ります**。フォルダ名が同じでも別のフォルダは統合しません。
フォルダに入っていること自体は測定条件の一致を意味しないため、比較可能な条件かを確認してください。

#### 前提と考え方

| 対象 | 計算 | 使い方 |
| --- | --- | --- |
| **5-HT** | 同じ MSI 座標の画素ごとに `5-HT 生値 / d4-5-HT 生値` | 比の分布を表示し、ROI で集計。この画素比そのものは絶対定量値ではありません |
| **DA / NE** | 切片ごとの係数 `k = D_ref / D_s` を、その切片の DA と NE に共通でかける | グループ内の測定間の共通変動を揃えるための切片間正規化。絶対定量ではありません |
| **d4-5-HT** | 補正値を作らず生値を使う | 内部標準の QC 用 |
| **その他の分子** | 補正なし | `Data` シートや生値表示で確認 |

`D_s` は、各切片の固定した基準領域にある、D4 QC を通過した d4-5-HT の中央値です。
`D_ref` は、**同じ補正グループ内**で利用者が **固定基準**として明示選択したデータの `D_s` の中央値です。
グループ内で共通なのは `D_ref` であり、**係数 `k` は切片ごとに異なります**。
一度保存した参照集合・`D_s`・`D_ref`・`k` はプロファイルに固定され、Viewer の選択、
並び順、後日のデータ追加で自動再計算されません。**DA と NE を分子ごとに中央値 1 へ揃える処理ではありません。**
基準データの一部が利用できない場合も、残りだけで基準を計算し直しません。

**DA / NE の補正後の数値と固定共通レンジは、そのグループ内の比較に使います。**
グループごとに独立した `D_ref` を使うため、別グループの補正値をそのまま同じ基準の数値として比較しないでください。
グループ間の比較には、共通 QC などの基準を共有する別の設計が必要です。

> **科学的な制約**: d4-5-HT は DA / NE の同位体標準ではないため、この係数が DA / NE にも
> 測定間差として共通に働くことを実験的に確認するまでは **暫定的なクロスアナライト正規化**です。
> d4 の散布量だけで、分子ごとの回収・誘導体化・イオン化差を補正できるわけではありません。

#### 設定の流れ

1. 対象の Viewer タブをすべて閉じ、Master で対象の第2階層フォルダ、またはその配下を開きます。
2. **`補正・正規化の設定`** を開き、**`補正グループ`** を確認して **`このグループの全データを読み込み`** を押します。
   第2階層またはその配下を開いていた場合は、そのグループが初期選択されます。
   第1階層では配下の候補、「すべて」では全候補から、**1グループずつ**選択します。
   フルパス・件数・所属を確認してください。Master の個別チェックにかかわらずグループ全件が対象です。
   読み込みに失敗したデータがある場合は名前と理由を確認し、解消してから進めます。
   対象を切り替えるときは **`別のグループを選ぶ`** でグループ選択へ戻ります。
3. 測定・散布バッチ ID、前処理・誘導体化条件 ID、プロファイル ID、revision を入力します。
4. 各データの 5-HT、d4-5-HT、DA、NE の対応を確認します。名称からの自動候補は確定値ではありません。
   同一切片・同一 MSI 座標、測定／散布／前処理条件の比較可能性を確認します。
   **HE と MSI の見た目の位置合わせでは、分子間の元 MSI 座標一致を代用できません。**
5. d4 最小信号、d4 飽和境界、最低有効率を実測と QC 根拠に基づいて入力します。
   飽和境界を空欄にすると、飽和していないことを判定できないため **暫定**扱いです。
   画面の最低有効率 `0.8` は設定例であり、検証済みの推奨値ではありません。
6. 基準領域を **共通 QC**、**対応する解剖学的 ROI**、**全測定領域**から選び、
   **このグループ内から**固定基準とするデータを 1 件以上明示選択します。「全測定領域」は推定組織領域ではなく、
   登録分子のいずれかが測定された足跡です。組織構成差の影響を受けるため暫定です。
7. Otsu に使う内因性分子（5-HT / DA / NE）を明示選択します。d4 等の内部標準は除外されます。
   選んだ入力分子が 1 つでも欠ける切片では、入力集合を黙って減らさず Otsu を利用不可にします。
8. **`計算結果をプレビュー`** で対象件数、算出可能・暫定・算出不可の件数、各切片の `D_s`、
   グループの `D_ref`、切片ごとの `k`、有効率、状態／理由、分子別の固定共通レンジを確認します。
9. **`確認してこの PC に保存`** でローカルに一括保存します。保存時の表示は「補正・正規化／固定共通レンジ」になり、
   Otsu は **OFF** に戻ります。プレビュー後に生値、対象集合、フォルダの名前・階層・所属が変わっていると
   保存せず、再読み込みを求めます。古いプレビューの一部だけを保存することはありません。
10. クラウド利用時は、ローカル保存後に **`このプロファイルをクラウドに保存`** を押します。
    各データの同時更新を確認して順に送り、部分失敗はデータ名と理由を表示します。
    **必ず全対象で同じプロファイル ID と revision の同期完了を確認し、異なる revision を混ぜて比較しないでください。**

生値、グリッド、固定参照 ROI、または計算条件が変わると、保存済みプロファイルは
`古い設定` として算出に使われません。revision を上げて再計算してください。

#### フォルダやデータを変更したとき

補正の作成時に使ったフォルダ・対象・基準は記録として保存し、現在の所属とは分けて管理します。
**フォルダの所属変更がクラウドへ未同期の間は、新しい補正設定を進められません。**
Master の同期状況から再試行するか、クラウドの所属へ戻して解消してください（[11-4](#11-4-viewer-の-クラウドに保存)）。

| 変更 | 補正の扱い |
| --- | --- |
| フォルダ名の変更 | 現在のパスを更新し、補正時の記録と係数を保持 |
| 同じ第2階層内での移動 | 補正グループと係数を保持 |
| 新しいデータの追加 | 新規データは補正未設定。グループは構成変更ありとなり、既存係数を自動変更しない |
| 別の第2階層への移動・対象外階層への移動 | 元の設定を記録として保持するが、移動先の補正として適用しない。移動先グループで再設定する |
| データ・フォルダの削除 | 残ったデータの所属を確認。基準や係数は自動再計算しない |
| 基準データ・固定基準 ROI・計算条件の変更 | 状態と理由を確認し、新しい revision で設定・計算する |
| 同じグループで設定の版が混在 | 全件適用済みとは扱わない。設定混在・同期未完了を確認し、同じ版へ揃える |

v2.9 の従来プロファイルは読み込み可能で、自動移行・自動再計算はしません。
フォルダ単位の設定に切り替える場合は、そのグループ全件を読み込み、新しい設定として保存してください。
同じプロファイル ID / revision に異なる内容は保存できません。この重複確認は他グループの設定も含みます。

ZIP 復元などで別々の第2階層フォルダが同じ補正グループ ID を持った場合は、重複・混在として表示します。
そのフォルダ全件の条件とプレビューを確認し、保存時に明示的に承認すると、
**そのグループに新しい ID とプロファイルを作成します**。もう一方のグループは変更しません。

#### 5-HT の ROI 絶対定量（任意）

絶対定量は、検証済みの線形検量線 `R = a × C + b`（`a > 0`）を登録した場合に限り、
ROI ごとに `C = (R - b) / a` で算出します。次のすべてが必要です。

- 検量線 ID、出典（資料／実験 ID）、検証済みの出力単位
- 傾き `a`、切片 `b`、応答 `R` の検証範囲、LLOQ / ULOQ
- 実験で検証した応答集計方式との完全一致：
  - `mean_pixel_ratio`：ROI 内の「画素ごとの 5-HT / d4 比」の平均
  - `ratio_of_sums`：同じ有効画素集合における 5-HT 信号合計 / d4 信号合計
- 試料の測定バッチ・前処理条件と検量線条件の一致、および組織からの回収・イオン化を含む妥当性確認

検量範囲外、LLOQ 未満、ULOQ 超過、有効率不足、条件不一致では値を空欄とし、理由コードを出します。
**範囲外への外挿、単位の自動変換、画素単位の絶対定量は行いません。**
d4 を散布したという事実だけでは、絶対定量の根拠になりません。

> **妥当性の範囲**: 本バージョンでは、実データの画素値や実際の検量線を用いた科学的検証は実施していません。
> ソフトウェアテストの合格は、DA / NE の正規化または 5-HT 絶対定量の科学的妥当性を保証しません。

---

## 4. 画面の見かた

起動すると、画面は大きく 3 列に分かれています。

```
┌── ヘッダー（データ名＋バージョン／Otsu／Export ほか） ───────┐
│                                                                  │
│  SPECIMEN   │ 生値／補正・正規化とレイヤ一覧 │  ANALYSIS          │
│  ROI LIST   │ Image Viewer（参照 ｜ 合成）  │    （棒グラフ）    │
│             ├───────────────────────────────┼────────────────────┤
│             │ IMAGES（サムネイル行）        │ Mass 測定情報      │
└──────────────────────────────────────────────────────────────────┘
```

| 領域 | 役割 |
| --- | --- |
| **ヘッダー** | 開いているデータ名とアプリのバージョン、`表示マスク(Otsu)` トグル、`Export` / `Import`、Master 画面へのリンク |
| **SPECIMEN** | 検体メタ情報（ID, Organism, Strain, Age, Sex） |
| **ROI LIST** | 解析対象 ROI の一覧・表示切替・手描き登録 |
| **表示値とレイヤ一覧** | Image Viewer のすぐ上。`補正・正規化` / `生値`、固定共通／個別レンジを切り替え、横一列のチェックボックスで表示レイヤを選ぶ。その下で選択レイヤの強度レンジ・透明度を調整する |
| **Image Viewer** | 複数画像を重ね合わせたメインキャンバス（パン／ズーム可）。参照用の画像（Atlas / 免疫染色）を登録してあるときは**左半分が参照画像、右半分が重ね合わせ**になります |
| **IMAGES** | 各ソース画像のサムネイル（クリックで表示切替）。Atlas と免疫染色は左端に並び、**押すと左半分の表示が切り替わります** |
| **ANALYSIS** | 選択中の ROI における化合物の平均強度を比較する棒グラフ |
| **Mass 測定情報** | 今後の機能拡張用エリア（現状はプレースホルダ） |

---

## 5. 基本ワークフロー

最初に試していただきたい流れです。

1. **Master 画面**で imzML + ibd と HE 画像を登録する（[3 章](#3-master-画面データ登録と一括管理)）。
2. 一覧の **開く** で Viewer を表示する。
3. 複数切片を比較する場合は、同じ補正対象を第2階層フォルダ内にまとめ、分子対応・MSI 座標・測定バッチを確認します。
   Master の **`補正・正規化の設定`** でグループを選び、固定参照、QC、必要なら 5-HT 検量線を明示設定します（[3-7](#3-7-補正正規化を設定する)）。
4. 係数・状態・理由をプレビューし、この PC へ保存します。クラウドを使う場合は、専用ボタンで同期し、全件が同じプロファイル / revision になったことを確認します。
5. 一覧の **開く** で Viewer を表示し、**`生値`** と **`補正・正規化`** を切り替えます。
6. Image Viewer 上の **レイヤ一覧**で、表示したい画像（HE・MSI など）にチェックを付ける。
7. **重ね合わせ** ボタンで HE と MSI の位置合わせを行う（[7 章](#7-he-と-msi-の位置合わせ重ね合わせ)）。
8. 左の **ROI LIST** から気になる ROI をクリック → Image Viewer 上にハイライト、ANALYSIS グラフが更新されます。
9. 画面上で独自の領域を解析したいときは `+新規` で **手描き ROI** を追加します。
10. 最後に **Export** で、作業状態ごと ZIP に保存します。次回は **Import** で続きから作業できます。

---

## 6. 画像の重ね合わせと表示調整

### 6-1. レイヤの選択

Image Viewer のすぐ上に、レイヤの **チェックボックスが横一列**に並んでいます。

```
☑ ●DA   ☐ ●NE   ☐ ●5-HT   ☐ HE Stain
─────────────────────────────────────────────
MSI (DA)  強度レンジ (生単位) [0]-[2342] ⟲  実測 0 〜 3977   透明度 ☑ [100]%
```

- 表示したい画像のチェックを**オン**にしてください。複数同時に選べます。
- 下段 **IMAGES** のサムネイルを **クリック** しても同じ効果です。選択中のサムネには青枠が付きます。
- 分子名の丸は、その分子を描く色です（[6-3](#6-3-重ね合わせのルール)）。
- **名前をクリック**すると、下の調整欄（強度レンジ・透明度）がそのレイヤのものに切り替わります。
  選ばれているチップには青い枠が付きます。**表示の ON/OFF は変わりません**（チェックとは別操作です）。

### 6-2. 拡大・縮小・移動（パン／ズーム）

| 操作 | 結果 |
| --- | --- |
| **マウスホイール** | カーソル位置を中心に拡大・縮小（20%〜800%） |
| **ドラッグ** | 表示位置を移動 |
| **2 本指ピンチ** | 指の中点を中心に拡大・縮小（タッチ操作） |
| **リセット** ボタン | 拡大・移動を初期状態に戻す |

拡大率は Image Viewer 右上に表示されます。ROI 手描き中はパン操作は無効になります。

### 6-3. 重ね合わせのルール

- **MSI は分子ごとの固定色 × 強度**で描き、**加算合成**します。何分子表示していても
  描き方は同じです。重なった所は色が混ざるので、**共局在**（同じ場所に出ているか）が読めます。

  | 化合物 | 色 |
  | --- | --- |
  | DA | 赤 |
  | 5-HT | 緑 |
  | NE | 黄 |
  | ACh | 橙 |
  | GABA | シアン |
  | Glu | マゼンタ |
  | 上記以外 | 無彩色（白） |

- 明るさは**測定値に比例**します（持ち上げ補正はありません）。弱い部分が見えにくいときは、
  [6-4](#6-4-強度と透明度の調整) の強度レンジの**上限を下げて**ください。
- 拡大しても**測定点の四角はそのまま**です（にじませません）。MSI の 1 ピクセルは
  1 回の測定なので、補間すると無い値が有るように見えてしまうためです。
- **HE / IF は MSI より下**に描かれます。加算合成なので、**信号の無い所では下の HE が
  そのまま見えます**。MSI を薄くしたいときは透明度を下げてください。
- **IMAGES のサムネイルも同じ色で焼かれます**。表示のチェックを付け外ししても
  サムネイルの絵は変わりません。

### 6-4. 強度と透明度の調整

チェックボックスの列のすぐ下に、**選択中のレイヤ**の調整欄が**常に表示**されています。
別のレイヤを調整したいときは、そのレイヤの**名前をクリック**してください。

| 項目 | 内容 |
| --- | --- |
| **強度レンジ** | 表示に使う下限・上限。MSI は生値表示なら **生単位**、補正表示なら比または正規化 a.u.、HE は **輝度 0-255** で指定します |
| **⟲（リセット）** | 自動で決まる既定値に戻す |
| **実測** | そのレイヤに実際に入っている値の範囲（表示のみ） |
| **透明度** | 0〜100%。**同じ種類のレイヤすべてに効きます**（MSI で変えれば全分子に、HE で変えれば HE/IF に）。チェックを外すとそのレイヤは常に不透明で描かれます |
| **モノクロ** | HE / IF のときだけ出ます（[6-7](#6-7-he-をモノクロで見る)） |

生値表示の既定値は、下限が実測の最小値、上限が **99 パーセンタイル** です（外れ値 1% を切ることで
弱い信号まで見えるようにしています）。補正・正規化表示では、プロファイル作成時に固定したレンジを使えます。

### 6-5. 生値と補正・正規化値の切り替え

レイヤ一覧の上にある **`生値`** / **`補正・正規化`** を押すと、その Viewer 内で即時に切り替わります。
有効または暫定のプロファイルがあり、過去の表示選択が無いデータは、補正・正規化表示で開きます。
利用できるプロファイルが無ければ生値表示です。

| レイヤ | 「補正・正規化」で表示する値 |
| --- | --- |
| 5-HT | 同じ元 MSI 座標の `5-HT / d4-5-HT` 画素比 |
| DA / NE | 生値に同じ切片係数 `k` をかけた正規化値 |
| d4-5-HT | QC 用の生値 |
| その他 | 補正対象外の生値 |

Viewer では補正グループ、作成時のフォルダ、設定の版と適用状態を確認できます。
別グループへ移動したデータの古い補正は適用されません。Master で現在のグループを再設定してください。

**`固定共通レンジ`** は、同じ第2階層グループでプロファイルを作った対象全体の有限値範囲を、分子別に固定して使います。
同じグループの切片どうしの色の明るさを比較するときはこちらを使ってください。
表示中のデータ集合や、読み込んだ ZIP の件数からレンジを再計算しません。
**`個別レンジ`** は各 Viewer に合わせて見やすくできますが、切片間で色の明るさを直接比較できません。
共通レンジが未設定の分子は個別表示になり、画面にその旨が出ます。

補正値を算出できない場合、**実測済みだが D4 QC などで無効の画素は灰色**、
**その分子を測定していない画素は透明**です。これらは Otsu による非表示とは別です。
画面に表示される状態・理由を確認し、不足する D4、座標、有効率、参照設定を Master で修正してください。

> **注意**: 補正・正規化画像は絶対定量画像ではありません。
> 5-HT の絶定量は、検量線と有効性条件を満たした **ROI の集計値だけ**です。

### 6-6. 表示マスク（Otsu）

ヘッダーの **`表示マスク(Otsu)`** は、Master で固定した内因性分子（5-HT / DA / NE から明示選択）の
**生値の和**を `log10(x+1)` した分布に大津の二値化を適用し、閾値未満のスポットを画面上だけ非表示にします。
d4 等の内部標準は入力から除外します。これはフルスペクトル TIC でも、登録した全レイヤの自動合計でもありません。
固定した入力分子が欠ける場合は、別の分子だけで黙って判定せず、Otsu を利用不可にします。

オンへの変更、保存済み設定の再適用、閾値変更の確定など、Otsu の表示結果を適用する前には
画面上に確認が出ます。**「はい」を明示的に押すまで適用されません。**
その確認では、**実際の組織内の弱い真の信号も隠れ、分布や強弱が歪んで見える可能性**を必ず確認してください。

オンにするとヒストグラムが表示されます。**赤い縦線をドラッグ**すると閾値を手動で
前後に動かせます（自動値から ±3 の範囲、log10 単位）。ドラッグ中はプレビューで、指を離した時の確認後に確定します。
灰色の破線が自動閾値、`⟲` で自動値に戻ります。非表示になった画素は**透明**になり、HE を同時表示していれば HE が見えます。

> **Otsu は表示だけです。** 生値、補正値、ROI の平均・SD・有効数、絶対定量、Excel の解析値は変えません。
> Otsu で隠れた画素も数値集計に含まれます。Excel の `Otsu_Visible` は表示判定の記録であり、組織所属や解析有効性の判定ではありません。

### 6-7. HE をモノクロで見る

HE / IF を選んで **モノクロ** にチェックを入れると、染色画像を**白黒**で表示します。
MSI の色と染色の色が混ざらないので、重ね合わせたときに MSI の分布が読み取りやすくなります。

- **既定はカラー**（元の染色そのままの色）です。
- 下の **IMAGES サムネイルも同時に白黒**になります。
- 設定はデータごとに保存され、ZIP に書き出しても保たれます。
- 元データは変更しません。チェックを外せばいつでもカラーに戻ります。

### 6-8. 表示を回転する

ヘッダ左側の **回転欄**（`両方 ▾` `0 °` `↻`）で、表示画像を回せます。切片が斜めに乗って
測定されたときなどに、向きをそろえるためのものです。

| 操作 | 内容 |
| --- | --- |
| **対象セレクタ** | `両方` / `HEのみ` / `MSIのみ` を選ぶ |
| **角度** | `-180`〜`180` の範囲で入力（`↑` `↓` キーでも 1° ずつ動きます） |
| **`↻`** | 回転・移動・拡大をすべて初期値に戻す |

**角度はデータごとに保存されます。** 一度合わせておけば、次に開いたときも同じ向きで出ます
（`クラウドに保存` を押せば他の PC でも同じ向きになります）。ZIP に書き出しても保たれます。

> **`HEのみ` / `MSIのみ` は何のためか**: HE と MSI が別々の向きで取り込まれていて、
> どうしても重ならないときに、片方だけを回して向きをそろえるためのものです。
> 通常は `両方` のままで構いません。

> **注意**: 回すと画像の角が枠からはみ出して切れることがあります。ホイールで縮小するか、
> ドラッグして位置を調整してください（`↻` でいつでも戻せます）。
>
> 下の **IMAGES サムネイルは回転しません**。回転が反映されるのは Image Viewer の表示だけです。

### 6-9. スケールバー

Image Viewer の**左下にスケールバーが常に表示されます**。

長さの根拠は **imzML に書かれている 1 画素の実寸**（`pixel size x` / `IMS:1000046`）です。
登録時にこの値を読み取って保存しているので、**すでに登録済みのデータでも、
何も操作しなくてもそのまま出ます**（ZIP から読み込んだデータ・クラウド経由のデータも同じ）。

- 長さは `1 / 2 / 5 × 10ⁿ` の中から、画面幅の 1/5 前後になるものが**自動で選ばれます**
- **拡大縮小に追随します**。200% に拡大すると、同じ画面上の長さが半分の実寸を表します
- 1000 µm 以上は **mm** で表記します（例 `2 mm`）
- **回転しても水平のまま**です（バーが斜めになったり文字が逆さになったりしません）

> **画素サイズを持たない imzML では表示されません。** 適当な長さを描くと
> 誤った寸法を示してしまうためです。Master の一覧に `/ 130 µm` のような表示があれば、
> そのデータには画素サイズが入っています。

### 6-10. 画像を保存する（PNG）

Image Viewer 右上の **`画像を保存`** を押すと、表示中の重ね合わせを
**スケールバー付きの PNG** で保存できます。

| | 保存される画像 |
| --- | --- |
| レイヤの表示 / 色 / 強度レンジ / 生値・補正切替 / Otsu | **反映されます**（画面と同じ） |
| 回転 | **反映されます** |
| ROI の線 | **反映されます** |
| 拡大・移動（ズーム / パン） | **反映されません**。常に**画像全体**が保存されます |
| スケールバー | 左下に**焼き込まれます** |

> ズームして見えている範囲だけを切り取りたい場合は、この機能では対応していません。

---

## 7. HE と MSI の位置合わせ（重ね合わせ）

Image Viewer 右上の **`重ね合わせ`** ボタンを押すと、位置合わせのウィンドウが開きます。
HE 画像と MSI が登録されているときだけ使えます。

左が HE、中央が MSI、右が現在の変換によるプレビュー（緑が HE の組織領域）です。
**いずれも「生の向き」で表示されます**（Viewer 表示の 90° 回転は掛かりません）。

### 7-1. 自動整合（シルエット）

**`自動整合 (シルエット)`** を押すと、HE の組織領域（彩度で判定）と MSI の組織領域
（総信号量への大津法で判定）の形を合わせにいきます。モーメントで初期値を作り、
主軸の 180° 不定性と鏡映の 4 通りから重なり（Dice 係数）が最大のものを選び、
角度・スケール・平行移動を細かく詰めます。

結果は **すぐには適用されません**。Dice・回転・スケール・現在値との差・期待誤差が
表示されるので、確認してから **適用** を押してください。キャンセルすればそのまま元に戻ります。

> ⚠ **組織が丸いときの注意**：切片の形が回転対称に近いと、どの角度でも重なり具合が
> ほとんど変わらず、回転角を決められません。この場合は赤字の警告が出ます。
> 警告が出たときは、次の対応点による方法で回転を確定してください。

### 7-2. 半自動整合（対応点）

1. HE 側の **`対応点を打つ`** を押し、目印になる場所をクリックします。
2. MSI 側の **`対応点を打つ`** を押し、**同じ場所を同じ順番で** クリックします。
3. **3 点以上**（できれば切片の外周に散らして）打ってから **`対応点から解く (Solve)`** を押します。

各点の残差が px と µm で表示され、**RMSE の 2.5 倍を超えた点は赤くハイライト**されます。
打ち間違いはここで見つけてください。`1 点戻す` / `全消去` でやり直せます。

> 2 点だけだと残差は必ず 0 になり、誤差が測れません。必ず 3 点以上打ってください。
> 点が狭い範囲に固まっていると外挿が効かないため、警告が出ます。

### 7-3. 自動と手動を合成する

対応点を打った状態で自動整合を実行すると、**`手動と合成して適用`** が選べる場合があります。
これは両者をそれぞれの期待誤差の逆数で重み付けして平均したもので、
精度の高いほうに寄った解になります。

両者が大きく食い違う（合成不確かさの 3 倍を超える）ときは、どちらかが壊れている
可能性が高いため、合成は提示されず両方の値がそのまま表示されます。

---

## 8. ROI の表示・選択・手描き追加

### 8-1. 既存 ROI の操作

| 操作 | 結果 |
| --- | --- |
| ROI 名をクリック | その ROI をアクティブ化し、ANALYSIS を更新 |
| ROI 行のチェックボックス | その ROI 単体の表示／非表示 |
| ヘッダの **`Show`** トグル | 全 ROI の一括表示／非表示 |

ROI の色はデータセット側で定義されており、手描きで追加した ROI には衝突しないパレット色が自動で割り当てられます。

### 8-2. ROI を手描きで追加する

1. ROI LIST 上部の **`+新規`** をクリックします。ボタンが `中止` に変わり、Image Viewer のカーソルが十字になります。
2. Image Viewer 内を順番にクリックして **頂点を 3 点以上** 追加します。
3. 確定は次のいずれかで行います。
   - 最初の頂点をもう一度クリック
   - ダブルクリック
   - **Enter キー**
4. 名前の入力ダイアログが出るので、任意の名称を入力します。
5. 頂点を打ち間違えたときは、ROI LIST 上部の **`1 点戻す`** ボタン、または
   **`Ctrl + Z`**（Mac は `Cmd + Z`）で **直前の 1 点だけ**を取り消せます。
   何度でも押せて、0 点まで戻しても描画モードは続くのでそのまま打ち直せます。
6. 全部やめたいときは **Escape キー** または `中止` ボタンを押してください。

追加した ROI は `User ROI 1, 2, …` として ROI LIST に登録され、Export した ZIP の
`Data/*.csv` に **0/1 の列** として、マニフェスト JSON に **座標** として書き出されます。

> ROI 座標は MSI ピクセル空間（生の列・行インデックス）で記録されるため、画面上の回転や拡大縮小には影響されません。

### 8-3. 離れた場所を 1 つの ROI としてまとめる

**同じ名前を付けた ROI は、1 つの ROI として扱われます。**

左右の皮質のように、**離れた 2 か所以上を同じ領域として測りたい**ときに使います。
2 か所目を描くときに、1 か所目と**同じ名前**を入力してください。

- ROI LIST では **1 行にまとまり**、名前の右に **`(2 箇所)`** のように箇所数が出ます
- Image Viewer には**すべての場所の輪郭**が表示されます
- ANALYSIS の平均は、**まとめたすべての場所の画素をひとまとめにして**計算されます
  （[9 章](#9-analysis棒グラフで比較する)）
- ZIP の CSV でも **1 本の列**になり、どちらの場所に入っていても `1` になります

> **前に別々の名前で作ってしまった場合**は、片方の名前を**もう片方と同じ名前に変更**して
> ください（名前をクリック、または `✎`）。確認が出たあと 1 つにまとまります。
>
> **同じ名前を付けたデータを開いた場合**も、開いた時点で 1 つにまとまって表示されます。

> **まとめた ROI の削除は、すべての場所がまとめて消えます。** 確認ダイアログに
> 「N 箇所すべてを削除しますか?」と箇所数が出ます。**一部の場所だけを消すことはできません。**

---

## 9. ANALYSIS（棒グラフ）で比較する

- ANALYSIS パネル上部の 3 つのプルダウンで、比較する化合物を最大 3 つまで選べます。`-` を選ぶとその系列は表示されません。
- ROI LIST から ROI をクリックするたびにグラフが更新されます。
- 各バーは、生値表示では **ROI 内の生値平均**、補正・正規化表示では対象分子の
  **比または正規化値の平均**を示します。分子ごとに単位と値域が異なるため、縦軸は分子ごとに独立です。
  棒の高さだけで異なる分子どうしの量を直接比較しないでください。
- **平均は「ROI に含まれる画素すべての平均」です。** 同じ名前でまとめた ROI
  （[8-3](#8-3-離れた場所を-1-つの-roi-としてまとめる)）では、すべての場所の画素を
  ひとまとめにして計算します。したがって**広い場所ほど平均に強く効きます**
  （場所ごとの平均をさらに平均する、という計算ではありません）。
  場所どうしが重なっていても、**重なった画素は 1 回だけ**数えます。
- 表には平均 ± SD（画素間の母標準偏差）と、`valid / measured / ROI` の数、有効率、状態、算出できない理由を表示します。
  SD は ROI 内の画素分布であり、切片間の誤差ではありません。
- `ROI`（geometry）は ROI 形状内の画素数、`measured` はその分子の有限な生値がある画素数、
  `valid` はそのうち D4 QC 等を通過して補正値を算出できた画素数です。有効率が設定下限未満なら、
  値を 0 で埋めず算出不可とします。
- 検証済み検量線の条件を満たす 5-HT では、ROI 絶対定量値と単位も表示します。範囲外や条件不一致では、
  外挿せず理由を表示します。DA / NE に絶対定量は適用しません。
- 未測定画素は平均に含まれません。補正対象では、D4 未測定、低信号、設定済み飽和境界以上などの無効画素も補正値集計に含めません。
- 棒の色は MSI の化合物カラー（6-3 表）に対応します。
- 値は保存済み生値またはそこから直接計算した導出値です。表示用の 0〜255 への量子化は通しません。
- **Otsu が非表示にした画素も、ANALYSIS の数値には含まれます。** Otsu 表示画素数は参考として別表示されますが、
  `valid` とは別の指標です（[6-6](#6-6-表示マスクotsu)）。

---

## 10. ZIP の書き出しと読み込み

### 10-1. Export（作業状態の復元用）

ヘッダー右上の **`Export`**（または Master 画面の各行の `Export`）で、
**1 つのデータを作業状態ごと** ZIP に書き出します。Master 画面の
**`選択を Export`** は、選んだデータごとにこの復元用 ZIP を書き出します。

| ファイル | 内容 |
| --- | --- |
| `<名前>.json` | マニフェスト。位置合わせ・強度／透明度・Otsu・ROI・補正プロファイル・補正グループの作成時記録と現在の所属・生値／補正の表示選択（`valueDisplay`）・**フォルダの位置** |
| `Data/<名前>.csv` | `x, y, <分子1>, <分子2>, …` の **生値**。各 ROI が 0/1 の列として付きます |
| `HE/<ファイル名>` | 登録した HE 画像の原本（TIFF なら TIFF のまま） |

生の `.imzML` / `.ibd` は含みません（1 分子あたり約 19 MB あるのに対し、
CSV の生値だけで表示・解析・再現に必要な情報はすべて揃うためです）。
CSV の数値は有効数字 9 桁で書かれ、読み込み時に **元の値と完全に一致** します。
補正値そのものは生値 CSV に焼き込まず、プロファイルと生値から再計算します。

### 10-2. 全データをExcel一括出力（解析用）

Master 画面の **`全データをExcel一括出力`** は、現在開いているフォルダや
チェックの有無にかかわらず、**すべてのフォルダの登録データ**を対象にします。
ダウンロードされるのは 1 つの ZIP で、中は次の構成です。

```text
marmoset_atlas_excel_YYYYMMDD_HHMMSS.zip
├── _Export_Index.xlsx
└── datasets/
    ├── 0001_<Unicode-safe-name>_<short-id>.xlsx
    └── 0002_<Unicode-safe-name>_<short-id>.xlsx
```

`_Export_Index.xlsx` は出力対象と結果の一覧です。`Index` シートでは、データ名・ID・
フォルダ・補正グループ・補正時のフォルダ・プロファイル ID / revision・対応するファイル・行数・分子数・**分子名一覧**・ROI 数・警告・エラーを
確認できます。`Molecules` シートには、すべての分子名をデータごとに 1 分子 1 行で
収録します。分子数が多く `Index` の 1 セルへ収まらない場合も、完全な一覧は
`Molecules` シートで確認できます。さらにシートの行数上限を超える場合は、
`Molecules_001`, `Molecules_002`, …へ自動的に分かれます。

`datasets` には、1 データにつき 1 つの Excel ファイルが入ります。日本語などの
データ名は保たれますが、ファイル名に使えない文字は置き換えられます。先頭の連番と
末尾の ID で同名も区別できます。

`datasets` 内の各 Excel ファイルは、生値と導出値を上書きせず別シートに収録します。

| シート | 内容 |
| --- | --- |
| **Data** | 1 から始まる `x`, `y`、保存されている全分子の MSI 生強度（Float32）、有効な ROI の所属を示す 0/1 列 |
| **Normalized_Data** | `Data` と同じ行・座標・ROI 所属に、5-HT 画素比、DA / NE 正規化値、分子ごとの状態／理由を付加。d4 と対象外分子の導出値は空欄なので `Data` を参照 |
| **ROI_Quantification** | ROI × 分子ごとの生値平均 / SD / n、正規化平均 / SD / n、5-HT 絶対定量、ROI / 実測 / 有効画素数、coverage、状態・理由コード、補正グループ・補正時のフォルダ・プロファイル ID / revision |
| **Normalization_Metadata** | 計算式と数値の扱い、補正グループ・補正時のフォルダ・現在の所属、プロファイル ID / revision / batch / QC / 固定参照 / 検量線 / 指紋、`valueDisplay`、Otsu のスナップショット、分子ごとの方法・単位・状態／理由 |
| **Metadata** | データ名・ID・フォルダ・グリッド・画素サイズ・方向・切片、分子数・ROI 数・出力時刻・アプリバージョン、警告、元の名前と出力列名の対応 |

Excel の 1 シートの行数上限を超える場合だけ、`Data_001`, `Data_002`, … と
`Normalized_Data_001`, `Normalized_Data_002`, … に自動で分かれます。ROI は同じ名前の領域を 1 列にまとめ、どれか 1 つに入る
画素を `1` とします。

**数値の扱い**

- 少なくとも 1 分子で有限の強度を持つ実測行だけを出力します。
- その画素で値を持たない分子（非有限値）のセルは空欄になります。
- `Data` には表示レンジ・色・回転・位置合わせ・Otsu・正規化を **適用しません**。
  v2.8 と同じ MSI 生強度をそのまま出力します。
- `Normalized_Data` の算出不可・無効値は **0 で埋めず空欄**にし、状態と理由コードを付けます。
- `Otsu_Visible` は `1` = 表示、`0` = 非表示、空欄 = Otsu 未適用または判定不可です。
  **表示記録のみ**であり、どの数値集計にも適用しません。保存済み Otsu 入力の指紋が現在の生値と一致しない場合は、
  `OTSU_SNAPSHOT_STALE` として `Otsu_Visible` を空欄にします。
- 対象分子として登録されているのに生値ラスタが壊れている／見つからない場合は、
  不完全な `Data` を出さないためそのワークブックをエラーにします。一方、d4 分子自体が未登録、または補正プロファイルが無い場合は、
  補正値を理由付きで空欄にし、**生値ワークブックは出力します**。

クラウド保存を使っている場合は、出力前に一覧を更新し、クラウドのみにある
データやクラウド側が新しいデータを順番に取得します。オフラインで一覧を
確定できないときは出力を開始しません。進捗は Master 画面に表示され、中止は
データとデータの間で受け付けます。未アップロードの変更とクラウド側の更新が
同じデータで競合している場合は、手元の値を上書きせず、そのデータだけ未出力にします。

一部のデータだけでエラーが起きた場合は、残りの処理を続けて失敗内容を表示します。
その場合、成功分だけの不完全な ZIP（`_PARTIAL.zip`）は、確認してからダウンロード
されます。全件に失敗した場合はファイルをダウンロードしません。

> この ZIP には HE・Atlas・免疫染色画像、元の `.imzML` / `.ibd`、作業状態は
> 含まれません。**解析専用であり、このアプリに Import できません。**
> 作業を復元するには [10-1](#10-1-export作業状態の復元用) の `Export` を使ってください。

### 10-3. Import（読み込み）

Master 画面の **`ZIP を読み込む`**、または Viewer の **`Import`** から読み込みます。
次の 2 形式に対応します。

| 形式 | 内容 |
| --- | --- |
| `marmoset_atlas_v1` | このアプリの Export が出す ZIP。**作業状態がそのまま復元されます** |
| `roi_bundle_v1` | 従来のビューアの `Download` ボタンが出した ZIP（`atlas.json` + xlsx/txt + TIFF） |

読み込んだデータは新しい登録データとして一覧に追加されます。
復元用 ZIP に v2.9 以降の補正プロファイルと `valueDisplay` が含まれていれば、生値／補正の表示選択も復元します。
読み込み後はプロファイル ID、revision、生値指紋、固定参照のスナップショットを検査し、不一致なら算出しません。
古い ZIP にこれらが無くても生値と従来の作業状態は読み込み、補正表示は利用不可として Master での設定を促します。

v2.10 のフォルダ単位プロファイルは、補正グループ ID、補正時のパス・対象データの記録、現在の所属も復元します。
**グループの一部や1件だけを読み込んでも、保存済みの `D_ref`・係数・固定共通レンジを保持します。**
新しいローカル ID や復元件数から基準を再計算することはありません。
既に別の補正グループとして使われているフォルダに取り込む場合は、グループ ID を黙って置き換えず、
所属・設定の状態を確認できるようにします。同じ補正として揃えるには Master で再設定してください。

**フォルダの位置も一緒に復元されます。** Export した ZIP には、そのデータが入っていた
フォルダのパス（例: `マーモセット / 2024年度`）が名前で記録されています。読み込むと同じ場所に
戻り、同じ名前のフォルダが無ければ自動で作られます。別の PC に移すときも階層が保たれます。

> ⚠ 復元用 ZIP は「別の PC やブラウザに持っていく」ための形式です。登録データ自体は
> **お使いのブラウザ内（IndexedDB）** にあり、ブラウザのデータを消去すると失われます。
> 大切な作業は Export して保管してください。

---

## 11. クラウド保存（複数の PC で同じデータを開く）

### 11-1. これは何か

以前は、登録したデータは**その PC のブラウザの中だけ**に保存されていました。
そのため、同じ URL を別の PC で開いても一覧は空のままでした。

クラウド保存を設定すると、登録したデータが自動でクラウド (Supabase) に送られ、
**どの PC でパスワードを入れても同じ一覧が出る**ようになります。

> 設定していない場合は、これまでどおりその PC の中だけで動きます。
> 設定のしかたは [`CLOUD_SETUP.md`](CLOUD_SETUP.md) を参照してください。

### 11-2. 使いかた

1. Master 画面を開くとパスワードを聞かれます。入れると一覧が出ます。
   - 「あなたの名前」は任意です。**誰が最後に保存したか**を表示するためだけの札で、
     入れなくても使えます。
2. データを登録すると、そのままクラウドにも送られます。
3. 別の PC で同じ URL を開き、同じパスワードを入れると、一覧にそのデータが出ます。
   `開く` を押した時点で中身が落ちてきます（一覧に出た時点ではまだ落ちていません）。

### 11-3. 一覧のバッジ

データ名の右の小さな札が、クラウドとの関係を表します。

| バッジ | 意味 | すること |
| --- | --- | --- |
| **同期済み** | クラウドと同じ内容が手元にある | なし |
| **未取得** | クラウドにはあるが、この PC にはまだ無い | `開く` で落ちてくる |
| **クラウドが新しい** | 他の PC で更新された | `開く` で最新に入れ替わる |
| **未保存の変更** | この PC で編集したが、まだ送っていない | Viewer の `クラウドに保存` |
| **未アップロード** | 登録はできたが、送信に失敗した | `アップロード` で送り直す |
| **クラウドで削除されました** | 他の PC で消された | `再アップロード` で戻すか、この PC からも削除 |

### 11-4. Viewer の「クラウドに保存」

ROI や重ね合わせ、表示の設定は、**作業中はこの PC の中だけ**に保存されます。
他の PC にも反映したいときは、ヘッダの **`クラウドに保存`** を押してください。

- 未保存の変更があるとボタンが**オレンジ色**になり、`(未保存)` と付きます
- 押すと数 KB だけ送られます（生データの ZIP は送り直しません。すぐ終わります）
- 画面をドラッグして動かしただけでは「未保存」になりません
  （どこを拡大して見ているかは、その PC ごとの好みなので共有しません）
- v2.9 以降は、同じ PC の古い Viewer タブが新しいローカル保存を黙って上書きしないように、
  読み込み時の改訂と保存直前の改訂を照合します。不一致ならローカル保存を止め、再読み込みを求めます。

補正プロファイルの複数データへの同期は、Viewer の通常保存ではなく、
Master の **`このプロファイルをクラウドに保存`** を使います。各データを改訂確認付きで順番に保存するため、
一部だけ失敗することがあります。成功件数と失敗したデータの理由を確認し、未完了分を再同期してください。
v2.10 では補正グループと現在の所属も同期します。フォルダの名前変更・移動は、
配下のクラウドのみにあるデータも対象です。オフラインや送信失敗で反映できなかった所属変更は、
Master の同期状況に記録され、画面を再読み込みしても残ります。設定の版が混在している間はグループ全件への適用完了とは扱いません。

| 同期状況の操作 | 動作 |
| --- | --- |
| **`同期を再試行`** | 未同期の所属変更を再送。クラウドが更新されていた場合は最新のパスと変更予定のパスを表示し、確認後に所属だけを変更する。最新のクラウド側の解析状態は保持 |
| **`クラウドの最新の所属に戻す`** | 未同期の所属変更を取り消し、クラウドの現在の所属に戻す。生値と保存済みの補正計算記録は保持 |

**所属の同期が未完了の間は、新しい補正設定を開始できません。** 上記のどちらかで解消してから、
対象グループと件数を確認し直してください。

### 11-5. 気をつけること

- **削除はすべての PC から消えます。** 1 台の掃除のつもりで消さないでください。
  削除の確認にもそう表示されます。
- **同じデータを 2 台で同時に編集するときは、クラウド側の更新に注意してください。**
  先に他の PC が保存していると、保存者と時刻を示して上書きするか確認します。
  利用者がその確認で明示的に承認した場合に限り、現在の手元の内容でクラウドを上書きします。
  これは、同じ PC 内の古いタブによる自動保存を止める保護とは別です。
  **v2.8 以前を開いた古いタブにはローカル改訂の保護がありません。作業前に他の Viewer タブをすべて閉じ、使うタブは強制再読み込みしてください。**
- 同じプロファイル ID / revision は同じ設定を意味します。Master は手元の保存データとクラウドの要約を照合し、
  同じ ID / revision に別の内容を保存しようとすると衝突として止めます。内容を変えるときは revision を増やしてください。
- `D_ref` と固定参照データの `D_s` / 生値指紋は保存済みスナップショットです。
  後に基準データが手元やクラウドから消えても、残ったプロファイルの `D_ref` を黙って再計算しません。
  基準を変える場合は新しい revision を作ってください。
- **パスワードを知っている人は全員が全データを読み書きできます。** 人ごとの権限はありません。
- **フォルダは、中にデータが入っているものだけが他の PC に再現されます。**
  空のフォルダは共有されません。
- **クラウドにつながらないときも作業は続けられます。** 手元のデータはそのまま開けます。
  Supabase が休止している場合はその旨が表示されるので、ダッシュボードで Restore してください。

---

## 12. キーボードショートカット

| キー | 機能 |
| --- | --- |
| `Enter` | 手描き ROI を現在の頂点で確定 |
| `Escape` | 手描き ROI を中止 |
| `Ctrl + Z` / `Cmd + Z` | 手描き ROI の直前の頂点を 1 つ取り消す |
| ホイール / ドラッグ | Image Viewer の拡大縮小 / 移動 |
| `Ctrl + F5` / `Cmd + Shift + R` | ブラウザ強制再読み込み（最新版取得） |
