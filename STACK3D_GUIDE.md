# 3Dモデルの使い方

Master右上の **3Dモデル** を開くと、登録済みのCor切片を番号順に並べて表示します。Masterで切片をチェックしてから開いた場合は、その選択範囲を使います。未選択の場合は登録済みのCor切片全体が対象です。

クラウドを使う場合は、まずMasterで通常どおりログインしてください。3D画面はAtlasの登録データを読み込みます。新しいPCでは初回に切片データの取得が必要です。

- DA・NE・5-HTのチェックで、単独表示・重ね合わせを切り替えます。
- ドラッグで回転、右ドラッグで移動、ホイールで拡大・縮小します。
- 右の**切片一覧のチェック**で、切片ごとの3D表示をON/OFFできます。OFFにするとMSI・HE・選択枠を隠し、その切片を共通色範囲の計算から外します。残りのONの切片に共通する色範囲を再計算するため、強い切片をOFFにすると他の切片の色も変わります。切片名はクリックして確認でき、**すべてON**で個別のOFFを解除できます。
- 切片範囲・不透明度・切片間隔を調整できます。切片間隔の初期値は**1.7倍**です。以前の初期値1.0倍を使っていたセッションも1.7倍へ移行します。変更して保存された他の間隔は保持します。
- 下のバーや左右の矢印を動かすと、選択断面より後（嗅球側）の切片を隠し、小脳側から選択断面までを表示します。開始位置を手動指定している場合はその範囲を使います。**表示範囲を戻す**で前後範囲を戻します。これらの操作は個別のOFFを解除せず、共通色範囲も変えません。
- 色の範囲は**全切片共通・分子ごと**に固定し、ONの切片を計算対象とします。OFFの切片も、他にONの切片があれば同じ色範囲のMSIプレビューで確認できます。全切片OFFのときはチェックを入れて再表示してください。
- **HEを重ねる**でHEのON/OFFを切り替え、HEの不透明度を調整できます。初期値はON・12%です。保存された位置合わせとHEの向きを使います。
- 右側は**MSI＋ROI・HE＋ROI・Atlas**の3タブです。MSIとHEに、保存されたROIの輪郭・色・表示設定を重ねます。**画像を拡大**でも同じ重ね合わせを確認できます。MSIは測定画素を保ち、HEは元画像から高解像度で描画します。Atlasは登録元画像を表示します。
- 右欄の左端の境界を**ドラッグ**すると幅を変えられます。MSI・HE・Atlasのプレビューの表示サイズと解像度は維持し、欄を狭くした場合はプレビュー下の横スクロールで画像全体を確認できます。境界をダブルクリックすると標準幅へ戻ります。境界にフォーカスして左右キーでも調整できます。幅は再読込や詳細画面から戻った後も保持します。狭い画面では通常のレスポンシブ配置を使います。
- **脳のイラストを重ねる**で半透明の脳模式図をON/OFFでき、濃さも調整できます。初期値はOFFです。模式図は切片と一緒に回転し、下のバーで切片を隠しても全体の輪郭を保ちます。全体が画面に収まらないときは**視点を戻す**を押してください。
- 未同期の編集は上部の小さな**⚠**で知らせます。マウスを重ねるか、クリックすると切片ごとの詳細を確認できます。
- 切片を選び、**この断面を開く** を押すと、その切片のAtlas詳細画面へ移動します。
- 詳細画面の **3Dモデルへ戻る** で、選択した断面と3Dの表示状態へ戻ります。

詳細画面で変更した表示・ROI等は、通常の保存方法に従います。描画中のROIや位置合わせは確定してから3Dへ戻ってください。戻る操作は、このPCでの保存処理の完了を待ちます。

## 配置と表示値

Corのグループ番号、切片番号の数値順で、小脳側から嗅球側へ並べます。欠番は詰めます。保存済みのXY画素サイズと回転を使い、必要に応じて切片ごとのXY移動・追加回転を調整できます。

切片間隔は**表示上の等間隔**です。実際の前後距離、脳体積、標準脳座標を表すものではありません。未測定部や反対側の補完は行いません。

分子・切片の表示切替や配置調整は原測定値を変更しません。切片のON/OFFでも保存済みのD4補正基準・係数・補正値を変更せず、元の切片位置と間隔を保ちます。補正値を表示する場合、補正できない切片・画素の原値を補正値に混在させません。色の範囲はONの切片で揃えますが、信号強度は絶対濃度を表すものではありません。補正基準が異なるグループを含む場合は、基準の違いも考慮してください。

3Dの配置は切片ごとの専用項目として保存され、クラウド保存と復元用ZIPでも保持されます。カメラ・表示分子・切片のON/OFF・選択断面などの閲覧状態は、そのブラウザーのセッションに保持し、再読込や詳細画面から戻った際に復元します。切片の追加時は新しい切片をONにし、既存の切片のON/OFFを引き継ぎます。

## 脳模式図の意味

脳のイラストは大脳・小脳・嗅球の概略を示す模式形状です。読み込んだ切片全体の表示範囲に合わせて大きさを調整します。個体の実測脳、標準脳アトラス、切片から再構成した脳表面ではありません。測定された片側データの左右を割り当てることや、反対側へ測定値を複製することは行いません。

実際の切片間距離を使っていないため、模式図との重なりから脳部位や体積を判定できません。表示中は「位置合わせ未実施」と明記し、保存するPNGにも **SCHEMATIC / NOT REGISTERED** を記載します。イラストのON/OFFや濃さは、詳細画面から戻っても保持します。

## 読み込めないとき

- Masterで対象の切片を開けることを確認してください。
- クラウドの認証が切れた場合はMasterからログインし直してください。
- 読み込めない切片は理由を表示します。表示枚数を確認してください。
- XY画素サイズが保存されていない切片は、実寸を推測せず読み込み対象から外します。
- WebGL 2対応ブラウザーと、3D描画を利用できる環境が必要です。
