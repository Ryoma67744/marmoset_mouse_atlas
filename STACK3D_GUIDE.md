# 3Dモデルの使い方

Master右上の **3Dモデル** を開くと、登録済みのCor切片を番号順に並べて表示します。Masterで切片をチェックしてから開いた場合は、その選択範囲を使います。未選択の場合は登録済みのCor切片全体が対象です。

クラウドを使う場合は、まずMasterで通常どおりログインしてください。3D画面はAtlasの登録データを読み込みます。新しいPCでは初回に切片データの取得が必要です。

- DA・NE・5-HTのチェックで、単独表示・重ね合わせを切り替えます。
- ドラッグで回転、右ドラッグで移動、ホイールで拡大・縮小します。
- 切片範囲・不透明度・切片間隔を調整できます。
- 切片を選び、**この断面を開く** を押すと、その切片のAtlas詳細画面へ移動します。
- 詳細画面の **3Dモデルへ戻る** で、選択した断面と3Dの表示状態へ戻ります。

詳細画面で変更した表示・ROI等は、通常の保存方法に従います。描画中のROIや位置合わせは確定してから3Dへ戻ってください。戻る操作は、このPCでの保存処理の完了を待ちます。

## 配置と表示値

Corのグループ番号、切片番号の数値順で、小脳側から嗅球側へ並べます。欠番は詰めます。保存済みのXY画素サイズと回転を使い、必要に応じて切片ごとのXY移動・追加回転を調整できます。

切片間隔は**表示上の等間隔**です。実際の前後距離、脳体積、標準脳座標を表すものではありません。未測定部や反対側の補完は行いません。

分子の表示切替や配置調整は原測定値を変更しません。補正値を表示する場合、補正できない切片・画素の原値を補正値に混在させません。切片別の色範囲は分布を見やすくする表示です。切片間で同色だから同じ絶対濃度、という意味ではありません。

3Dの配置は切片ごとの専用項目として保存され、クラウド保存と復元用ZIPでも保持されます。カメラ・表示分子・選択断面などの閲覧状態は、そのブラウザーのセッションに保持します。

## 読み込めないとき

- Masterで対象の切片を開けることを確認してください。
- クラウドの認証が切れた場合はMasterからログインし直してください。
- 読み込めない切片は理由を表示します。表示枚数を確認してください。
- XY画素サイズが保存されていない切片は、実寸を推測せず読み込み対象から外します。
- WebGL 2対応ブラウザーと、3D描画を利用できる環境が必要です。
